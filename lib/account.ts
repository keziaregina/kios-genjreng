export const MAX_NAME_LENGTH = 60;
export const MIN_PASSWORD_LENGTH = 8;

// Registration and settings judge the same addresses, so the pattern is declared once.
export const EMAIL_PATTERN = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
